version: 0.2

phases:
  install:
    commands:
      - echo Instalando dependencias...
  build:
    commands:
      - echo "Preparando archivos para el S3..."
artifacts:
  files:
    - 'pagina/**/*' # Esto toma todo lo que esté en la carpeta pagina
  discard-paths: no